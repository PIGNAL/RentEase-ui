class Car {
  id: number;
  type: string;
  model: string;
  constructor(
    id: number,
    type: string,
    model: string,
  ) {
    this.id = id;
    this.type = type;
    this.model = model;
  }
}